syntax = "proto3";

package monadiam.kafka.blacklist.v1;

message BlacklistValue {
    BlacklistTopicAction action_type = 1;
    BlacklistPayload payload = 2;
}

enum BlacklistTopicAction {
    ADD = 0;
}

message BlacklistPayload {
    double expires_at = 1;
    string session = 2;
}
