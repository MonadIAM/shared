syntax = "proto3";

package monadiam.kafka.account.v1;

message AccountValue {
    AccountTopicAction action_type = 1;
    AccountPayload payload = 2;
}

enum AccountTopicAction {
    CREATE = 0;
    PURGE = 1;
}

message AccountPayload {
    string account = 1;
}
